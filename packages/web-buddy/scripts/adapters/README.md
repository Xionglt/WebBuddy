# Runtime Adapters

Adapters connect external agent runtimes to this package's browser tools.

```text
adapters/
└── claude-code/   # Claude Code recovered runtime path using Playwright MCP.
```

These adapters are not the project's self-owned runtime. The local runtime lives
under `src/runtime/local/`.

