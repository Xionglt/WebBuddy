#!/usr/bin/env node
/**
 * Compatibility wrapper.
 *
 * The Claude Code runtime adapter lives under scripts/adapters/claude-code so
 * it is visibly separate from the self-owned local Web Agent runtime.
 */
import './adapters/claude-code/alibaba-apply.mjs'
